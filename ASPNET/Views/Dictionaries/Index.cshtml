@model IEnumerable<ASPNET_EF.Models.Dictionaries>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DictionaryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DictionaryLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDefaultDictionary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DictionaryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DictionaryLevel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDefaultDictionary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
